@model CategoryVM

<div class="row">
    <div class="col-lg-12">
        <div class="card">
           
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <h2 class="card-title">Category List</h2>
                    <a asp-controller="Category" asp-action="Upsert" class="btn btn-light d-flex align-items-center">
                        <span class="material-symbols-sharp">
                            add_circle
                        </span>
                        Create New Category
                    </a>
                </div>
                <table id="myTable" class="table table-striped table-bordered dt-responsive nowrap" style="border-collapse: collapse; border-spacing: 0; width: 100%;">
                    <thead>
                        <tr>
                            @* <th>Id</th> *@
                            <th>Name</th>
                            <th>Display Order</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody class="category-body">
                        @* @foreach (var obj in Model.Categories!.OrderBy(u => u.DisplayOrder))
                        {
                            <tr>
                                <td>@obj.Id</td>
                                <td>@obj.Name</td>
                                <td>@obj.DisplayOrder</td>
                                <td>
                                    <div class="actions">
                                        <a class="btn btn-light" asp-controller="Category" asp-action="Upsert" asp-route-id="@obj.Id">
                                            <span class="material-symbols-sharp">
                                                edit_square
                                            </span>
                                            <span>Edit</span>
                                        </a>

                                        <a class="btn btn-danger" asp-controller="Category" asp-action="Delete" asp-route-id="@obj.Id">
                                            <span class="material-symbols-sharp">
                                                delete
                                            </span>
                                            <span>Delete</span>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        } *@
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts{
    <script src="~/js/category.js" ></script>
}
